<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.0.5</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>
    <groupId>org.camunda.community</groupId>
    <artifactId>orchestration-api-client-java</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>orchestration-api-client-java</name>
    <description>orchestration-api-client-java</description>
    <properties>
        <java.version>25</java.version>
        <camunda.version>8.9.0</camunda.version>
        <springdoc.version>3.0.3</springdoc.version>
        <libre-wsdl4j.version>1.12.0</libre-wsdl4j.version>
    </properties>
    <dependencies>

        <!-- ===== Spring Boot Starters ===== -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web-services</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.ws</groupId>
            <artifactId>spring-ws-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- ===== Camunda Java Client ===== -->
        <dependency>
            <groupId>io.camunda</groupId>
            <artifactId>camunda-client-java</artifactId>
            <version>${camunda.version}</version>
            <exclusions>
                <!--
                  Exclude all Jackson artifacts bundled by camunda-client-java to avoid
                  version conflicts. Jackson 2.21.x remains on the classpath via
                  springdoc-openapi-starter-webmvc-ui and is used by this project.
                -->
                <exclusion>
                    <groupId>com.fasterxml.jackson.core</groupId>
                    <artifactId>jackson-databind</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>com.fasterxml.jackson.core</groupId>
                    <artifactId>jackson-core</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>com.fasterxml.jackson.core</groupId>
                    <artifactId>jackson-annotations</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>com.fasterxml.jackson.datatype</groupId>
                    <artifactId>jackson-datatype-jsr310</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>com.fasterxml.jackson.dataformat</groupId>
                    <artifactId>jackson-dataformat-yaml</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <!-- ===== Jackson 2.21.x (transitively provided by springdoc-openapi) ===== -->

        <!-- ===== JAXB / XML Binding ===== -->
        <dependency>
            <groupId>jakarta.xml.bind</groupId>
            <artifactId>jakarta.xml.bind-api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.glassfish.jaxb</groupId>
            <artifactId>jaxb-runtime</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- ===== Swagger / OpenAPI UI ===== -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>${springdoc.version}</version>
            <!--
              NOTE: springdoc-openapi 3.0.2 is compiled against com.fasterxml.jackson 2.x
              (via swagger-core-jakarta). Excluding those artifacts would cause a
              ClassNotFoundException at runtime. Jackson 2.21.x therefore remains on the
              classpath for springdoc's own use; our application code uses tools.jackson
              (Jackson 3.x) provided by spring-boot-starter-web.
            -->
        </dependency>

        <!-- ===== SOAP / WSDL support ===== -->
        <!-- Source: https://mvnrepository.com/artifact/io.github.librewsdl4j/libre-wsdl4j -->
        <dependency>
            <groupId>io.github.librewsdl4j</groupId>
            <artifactId>libre-wsdl4j</artifactId>
            <version>${libre-wsdl4j.version}</version>
            <scope>runtime</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <jvmArguments>--enable-native-access=ALL-UNNAMED</jvmArguments>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>